[package]
name = "hiveguard-plugin-cti-geoip"
version = "0.1.0"
edition = "2021"

[dependencies]
hiveguard-plugin-api = { path = "../../hiveguard-plugin-api" }
hiveguard-cti = { path = "../../hiveguard-cti" }
serde = { version = "1", features = ["derive"] }
tracing = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
