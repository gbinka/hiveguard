{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "source.kafka",
  "description": "Apache Kafka consumer log source. Full schema delegated to hiveguard_core::config::KafkaSourceConfig — this top-level shape only enforces presence of required fields.",
  "type": "object",
  "additionalProperties": true,
  "required": ["brokers", "topics", "group_id"],
  "properties": {
    "brokers":  { "type": "array", "items": { "type": "string" }, "minItems": 1 },
    "topics":   { "type": "array", "minItems": 1 },
    "group_id": { "type": "string", "minLength": 1 }
  }
}
