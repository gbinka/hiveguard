# notifier.email

## ⚠️  NOT IMPLEMENTED YET

Plugin scaffold for Email notifier. `init` returns `PluginError::Init` so
operators don't accidentally configure a no-op notifier. Use `notifier.webhook`
with a custom `template` as a stand-in until this is fleshed out.
