[package]
name = "hiveguard-plugin-detector-scanner-fingerprint"
version = "0.1.0"
edition = "2021"
description = "Scanner fingerprint detector plugin for HiveGuard"

[dependencies]
hiveguard-plugin-api = { path = "../../hiveguard-plugin-api" }
hiveguard-core = { path = "../../hiveguard-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }
